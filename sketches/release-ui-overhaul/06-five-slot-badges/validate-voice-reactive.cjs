const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const {JSDOM,VirtualConsole}=require('jsdom');const source=fs.readFileSync(path.join(__dirname,'index.html'),'utf8');const errors=[],vc=new VirtualConsole();vc.on('jsdomError',error=>errors.push(error));let contextClosed=false,sourceDisconnected=false;
const dom=new JSDOM(source,{runScripts:'dangerously',url:'https://preview.test/voice-reactive',pretendToBeVisual:true,virtualConsole:vc,beforeParse(window){window.matchMedia=()=>({matches:false,addEventListener(){},removeEventListener(){}});window.navigator.mediaDevices={getUserMedia:async()=>({getTracks:()=>[{stop(){}}]})};class Recorder{static isTypeSupported(){return true}constructor(_stream,options={}){this.mimeType=options.mimeType||'audio/webm';this.state='inactive'}start(){this.state='recording'}stop(){this.state='inactive';this.ondataavailable?.({data:new window.Blob(['audio'],{type:this.mimeType})});this.onstop?.()}}window.MediaRecorder=Recorder;class AudioContext{constructor(){this.state='running'}resume(){return Promise.resolve()}createAnalyser(){return{fftSize:512,smoothingTimeConstant:0,connect(){},disconnect(){},getByteTimeDomainData(samples){for(let i=0;i<samples.length;i++)samples[i]=i%2?164:92}}}createMediaStreamSource(){return{connect(){},disconnect(){sourceDisconnected=true}}}close(){this.state='closed';contextClosed=true;return Promise.resolve()}}window.AudioContext=AudioContext;window.Audio=class{play(){return Promise.resolve()}pause(){}};window.URL.createObjectURL=()=> 'blob:test';window.URL.revokeObjectURL=()=>{}}});const d=dom.window.document,wait=ms=>new Promise(resolve=>dom.window.setTimeout(resolve,ms));
(async()=>{assert.equal(d.querySelectorAll('#voiceVisual .voice-orb').length,3);assert.equal(d.querySelector('.wave'),null,'decorative waveform remains');assert.match(source,/createMediaStreamSource\(stream\)/);assert.match(source,/getByteTimeDomainData\(samples\)/);assert.match(source,/Math\.sqrt\(energy\/samples\.length\)/);assert.match(source,/prefers-reduced-motion: reduce/);d.querySelector('[data-tab="log"]').click();d.getElementById('startRecording').click();await wait(80);const visual=d.getElementById('voiceVisual'),level=Number(visual.style.getPropertyValue('--voice-level'));assert.equal(visual.dataset.reactive,'true');assert.ok(level>.04,`expected microphone-driven level, got ${level}`);d.getElementById('stopRecording').click();await wait(30);assert.equal(visual.dataset.reactive,'false');assert.equal(visual.style.getPropertyValue('--voice-level'),'.04');assert.ok(contextClosed);assert.ok(sourceDisconnected);assert.deepEqual(errors,[]);console.log('DREAMWORLD_AUDIO_REACTIVE_FIGURE_VERIFIED');dom.window.close()})().catch(error=>{console.error(error);dom.window.close();process.exitCode=1});
